//import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
//import 'package:eventapp_mobile/main.dart';

void main() {
  testWidgets('Hello W', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    // await tester.pumpWidget(const MyApp());
    //expect(find.text('Hello World:) So far nothing'), findsOneWidget);
  });
}
